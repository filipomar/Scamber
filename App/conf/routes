# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Authentication services/pages
GET     /login                      					controllers.AuthenticationController.loginPage()
POST	/login											controllers.AuthenticationController.authenticate()

# Home page
GET		/												controllers.ApplicationController.index()

# User
GET		/user/picture/$account<[0-9A-Za-z_]{6,10}>		controllers.UserController.userPicture(account)

# Items
POST	/items/new 										controllers.ItemController.newItems()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               					controllers.Assets.at(path="/public", file)